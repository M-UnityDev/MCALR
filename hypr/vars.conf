$ayugrm = ayugram-desktop
$browser = zen-browser
$terminal = kitty
$fileManager = kitty --hold spf
$menu = wofi --show drun
$cliphist = cliphist list | wofi --dmenu | cliphist decode | wl-copy  #launch cliphist clipboard manager
$grim = grim -g "$(slurp)" - | wl-copy
$hyprpickr = hyprpicker -n -a -z
